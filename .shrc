# Pull in global shell config
test -r /etc/shrc && . /etc/shrc

alias lc='ls -C'
alias ll='ls -l'
alias ls='ls -1A --color=auto'
alias rm='rm -d'

set -o physical
set -o vi

HISTCONTROL=ignoreboth
HISTFILESIZE=10000
HISTSIZE=12000
PSC=τ
